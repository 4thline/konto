package org.fourthline.konto.test.report;

import org.fourthline.konto.test.IntegrationTest;
import org.testng.annotations.Test;

/**
 * @author Christian Bauer
 */
public class ReportActivityTest extends IntegrationTest {

    @Test
    public void foo() {
/* TODO
        getCurrentSession().beginTransaction();
        ReportActivity activity =
                new ReportActivity(null,
                                   placeController,
                                   eventBus,
                                   reportServiceAsync,
                                   ledgerServiceAsync,
                                   new GlobalSettings(settingsServiceAsync, eventBus)
                );
        assert activity != null;
        getCurrentSession().getTransaction().commit();
*/
    }
}
